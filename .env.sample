# VS Code Tunnel Configuration
# Add this file to .gitignore to keep host paths private

# Root path that will persist the VS Code tunnel data (tokens, etc.)
VSCODE_DATA_PATH=/path/to/vscode-tunnel

# Optional: Custom tunnel name
TUNNEL_NAME=pretty-pretty-tunnel-name

# Path to your development projects on host
VSCODE_REPOS=/path/to/your/projects


